var test_word = gon.test;
alert(test_word);

particlesJS("particles-js-1", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000"
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: polygon_1_number,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-2", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: polygon_2_number,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-0", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: polygon_0_number,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-4", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: 6,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-5", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: 6,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-6", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: 6,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-7", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: 6,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-8", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: 6,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-9", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: 6,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});

particlesJS("particles-js-10", {
  particles: {
    //シェイプ数
    number: {
      //要素内に表示するシェイプの数
      value: 1,
      density: {
        //シェイプ表示間隔設定
        enable: false,
        //シェイプ表示間隔指定
        value_area: 1000,
      },
    },
    //シェイプ色
    color: {
      value: "#000000",
    },

    //シェイプの設定
    shape: {
      //シェイプ形
      type: ["polygon"],

      //シェイプボーダー設定
      stroke: {
        //ボーダー幅
        width: 0,
        //ボーダー色
        color: "#000000",
      },
      //シェイプの形でpolygonを指定した場合
      polygon: {
        //ポリゴン角数指定
        nb_sides: 6,
      },
    },

    //シェイプ透過率指定
    opacity: {
      //透過率指定
      value: 0.5,
    },

    //シェイプサイズ
    size: {
      //シェイプサイズ指定
      value: 100,
    },

    //シェイプの動きの設定
    move: {
      //動きを制御するか
      enable: true, //true:有効, false:無効
      //動く速度
      speed: 1,
      //動く方向
      direction: "top-right", //none, top, top-right, right, bottom-right, bottom, bottom-left, left, top-left
      //ランダム設定
      random: false, //true:有効, false:無効
      //静止状態にする
      straight: false, //true:有効, false:無効
      //シェイプの動き
      out_mode: "bounce", //ボックス内で動かす bounce ボックス外に逃がす out
      attract: {
        enable: true,
        rotateX: 1000,
        rotateY: 300,
      },
    },
  },

  interactivity: {
    detect_on: "canvas",
  },
  //Retina Display対応
  retina_detect: true, //true:有効, false:無効
});
