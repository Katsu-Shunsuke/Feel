<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ホーム</title>
  <link href="https://fonts.googleapis.com/css2?family=Sawarabi+Gothic&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/css/home.css">
  <script src="assets/js/particles.js"></script>
	<script type="text/javascript" src="assets/js/setting.js"></script>
</head>
<body>
  <div class="post">
    <h1><%= @user.name %>さんの投稿
      <% if @user.id!=current_user.id %>
        <% follow=Follow.where(follow_id: current_user.id) %>
        <% if follow.find_by(opponent_id: @user.id).nil?%>
          <a href="/follow/<%= @user.id %>">フォロー</a>
        <% else %>
          <a href="/remove/<%= @user.id %>">フォロー解除</a>
        <% end %>
      <% end %>
    </h1>
    <h2>全<%= @posts_user.count %>個</h2>

    <div class="background">
			<script type="text/javascript">
				var polygon_number={};
				var speed_number={};
				var opacity_number={};
				var color_number={};
				var size_number={};
				var speeds=[0,5,4,3,2,1];
				var opacity=[0,0.8,0.6,0.4,0.2,0.1];
				var colors=["#","#ff0000","#00ff00","#0000ff","#ffff00","#00ffff","#ff00ff","#000000","#ffffff"];
				var sizes=[0,200,100,50];
			</script>
      <section id="particles-js">
        <script type="text/javascript" src="assets/js/setting.js"></script>
      </section>
	  </div>
  </div>
</body>
</html>